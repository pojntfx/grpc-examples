# Build container
FROM debian

# Setup environment
RUN mkdir -p /data
WORKDIR /data

# Add development dependencies
RUN apt update
RUN apt install -y inotify-tools

# Start developmen>t
COPY . .
CMD ./Hydrunfile dev
